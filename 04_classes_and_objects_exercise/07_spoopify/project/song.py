class Song:
    def __init__(self, name: str, length: float, single: bool):
        name = name
        length = length
        single = single

    def get_info(self):
        return f"{name} - {length}"
